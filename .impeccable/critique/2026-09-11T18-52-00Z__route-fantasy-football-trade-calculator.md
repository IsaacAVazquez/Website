---
target: fantasy football trade calculator
total_score: 36
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
timestamp: 2026-09-11T18-52-00Z
slug: route-fantasy-football-trade-calculator
---
Method: dual-agent re-score after the 2026-09-11 remediation; compare against route-fantasy-football-trade-calculator.md (2026-09-11T09-00-19Z). Half A drove the route in Chromium at 1440 and 390, light and dark, idle and populated (Chase for Gibbs typed through both comboboxes with "WR" then "RB", plus Chase for Bijan Robinson, Chase for McCaffrey, and Chase for Rachaad White by share params), with `scroll-behavior` forced to `auto` and the snapshot stamps awaited before any measurement, and scored the ten heuristics before re-reading the earlier scores. Half B took each before P0 and P1 and the two P2s the commit says it closed, measured them in the browser or ran the engine directly against the committed `public/data/fantasy/ppr.json` with `now` pinned to today and to 2026-09-15, and read the diff of commit 6d12f70 with line numbers. Screenshots and the raw JSON are in `rescore/route-fantasy-football-trade-calculator/` and `rescore/trade-rescore.json`; the scripts are `rescore/trade-rescore.mjs`, `rescore/trade-extra.mjs`, and `rescore/trade-engine-rescore.ts`.

# Critique: Fantasy Football Trade Calculator (/fantasy-football/trade-calculator)

Scope note: this is a post-remediation re-score. The 09:00 critique scored the surface 30 of 40 in its first in-season state with one P0 and two P1, all upheld two to zero; the remediation for every upheld P0 and P1 merged to main in commit 6d12f70 (plus the 44px follow-up on the note links) and the dev server serves it. The snapshot is unchanged since that critique (`adpSource.asOf` 2026-09-10, 3,631 drafts, expert board 2026-09-10T00:19Z), and `getNflRegularSeasonWeek` still returns 1, so every in-season branch renders. I did not re-raise anything on the context file's decided or refuted lists, and the load-time "stale sources" flash that appeared in four of my first-pass cells is the refuted finding, so those cells were re-driven with a wait on the stamps rather than filed.

## Design Health Score

| # | Heuristic | Score | Key Issue | Before |
|---|-----------|-------|-----------|--------|
| 1 | Visibility of System Status | 4 | The note names which of two feed states the page is in with the Sep 10 date, the amber chip and the amber note now agree, and on the phone the verdict is on screen the moment it changes; "Preseason redraft · Model v1" still sits 90px above "Week 1 of the 2026 season" | 2 |
| 2 | Match System / Real World | 3 | Verdict sentences are plain and the 15% lines give the scale a vocabulary; "index" under an 85.0 is still never defined inline, and "limited coverage" is model vocabulary explained only behind a disclosure | 3 |
| 3 | User Control and Freedom | 4 | Swap, per-row remove, the timed two-step clear, URL settings, versioned storage, and a jump from the strip to the evidence; the strip itself cannot be dismissed | 4 |
| 4 | Consistency and Standards | 4 | Note, engine, and model doc say the same thing; the idle rail agrees with the header; balanced wears ink; the header prints minutes while the rail wraps them | 2 |
| 5 | Error Prevention | 3 | From Week 1 the engine caps coverage so a clear edge cannot appear; the gate is still date-only, so a thinning window keeps pricing until the stamp is four days old, and the withheld state still cannot be reached in production | 3 |
| 6 | Recognition Rather Than Recall | 3 | The drawn 5% band and 15% lines let the reader see the threshold instead of remembering it; package totals are still printed twice and the phone strip makes a third copy of the verdict | 3 |
| 7 | Flexibility and Efficiency | 4 | Keyboard combobox end to end, shareable URL, and now the weekly and waivers links every sibling has; "Evidence" jumps to the rail | 3 |
| 8 | Aesthetic and Minimalist Design | 3 | The rewritten note is 115 words and 341px tall at 390; the strip covers the rail's own header when its link lands and stays pinned while the rail is on screen; the centre column and the one-for-one fit table are unchanged | 3 |
| 9 | Error Recovery | 4 | Limited coverage opens to one sentence that names the season as the cause; the stale branch of the note names the date and the four-day window | 4 |
| 10 | Help and Documentation | 4 | The note explains both states and hands off to what is live; the doc describes the real gate; the rail's withheld body still hedges across three causes | 3 |
| **Total** | | **36/40** | **Excellent band (36 to 40): the seam is closed, the remaining work is distillation** | 30 |

All ten heuristics applied (Operate surface).

## Design Specificity Verdict

Mechanically the surface is the same desk as this morning. 25 driven cells at 1440 and 390 in both themes, idle and populated, with zero console errors, zero horizontal overflow, one `main`, one visible `h1`, one Instrument Serif element (the limits line), and the two note links measuring 95x44 and 93x44 at 1440 and 85x44 and 83x44 at 390 after the follow-up commit. Dark parity holds in every cell I opened, including the new strip (paper-raised ground, ink title, muted mono coverage line). What changed visually is concentrated in three places. The balance figure now draws a 38px shaded band around centre and two labelled 15% lines at the inner ticks, so the scale reads as an instrument with a stated tolerance rather than a bare rule. The note under the header grew from 52 to 115 words and gained two underlined links. Below `lg` a 58px verdict strip pins under the site header once both sides hold a player. The desk's authored character (mono readouts, hairline plates, first-person verdict sentences, signal spent only on the selected scoring plate, focus, and the marker) is intact, and the balanced verdict losing its amber makes the rail calmer than before. The 8-unit "15%" labels render at 9.5px at 1440 and 10.8px at 390 at 62% ink, which I computed at about 4.8:1 on the light rail ground; they are inside an `aria-hidden` SVG whose figure carries the sentence, so this is a legibility note and not an AA one.

## What closed

1. [P0] The page promises to withhold a verdict and then issues one. Closed. The note now reads "The mock-draft feed behind it was still sampling real drafts as of Sep 10, 2026, so the estimate still runs, but it answers what these players would cost in a draft this week rather than what they are worth in an in-season trade, and from Week 1 it reports balanced or leaning at most and never a clear edge. Once that feed stops updating the verdict is withheld on its own" (`trade-calculator-client.tsx:413-444`, rendered identically in all four theme and width cells), with a second branch for a stale feed that prints the last sample date and the four-day window (`:415-422`). The engine now derives the week from the snapshot season and `now` and pushes `FANTASY_TRADE_IN_SEASON_WARNING` while the market is usable (`fantasyTrade.ts:565-575`), and any warning caps coverage at limited (`:630-631`), so run directly against the committed snapshot with `now` = 2026-09-11 Chase for Gibbs returns `coverage: limited, verdict: balanced, gap 0.04` with the season sentence as its one warning, Chase for Watson returns `limited, leans-side-a, gap 0.90` where it returned `supported, clear-edge` this morning, and with `now` = 2026-09-15 it returns `insufficient` with the two stale-feed warnings. On screen, Chase for Gibbs renders "limited coverage" on the amber wash under the amber note, "Balanced offer" in ink, and "Why coverage is limited" opens to the season sentence. The doc paragraph at `docs/FANTASY_DRAFT_MODEL.md` "What happens to the trade calculator in season" now describes the four-day age gate, the Week 1 cap, and the recorded thinning-sample gap, and the coverage table's Limited row adds the season condition. Three artifacts, one position.

2. [P1] An in-season visitor is not routed to anything live. Closed. The note ends "Ranks that still move are on the weekly board, and this week's pickups are on the waivers page." with anchors to `/fantasy-football/weekly` and `/fantasy-football/waivers` (`trade-calculator-client.tsx:435-443`), measured in the `[role="note"]` at 95x44 and 93x44 at 1440 and 85x44 and 83x44 at 390 in both themes, and the header action stays "View rankings" to `/fantasy-football?position=overall&scoring=ppr`, which both verifiers asked for. At 390 the two links are the ninth and tenth tab stops, right after "View rankings".

3. [P1] The memorable moment does not happen. Closed, with one new P2 that fell out of the phone half. The track is scaled to the thresholds at 320 viewBox units per whole point of gap, a drawn band of plus or minus 16 units, 15% lines at the inner ticks (72 and 168), and a clamp at 96 units (`trade-balance-scale.tsx:12-24`). Measured at 1440 where the SVG renders 286px (1.192px per unit): Chase for Gibbs (gap 0.04) sits at `translateX(0px)` inside a 38px band; Chase for Bijan Robinson (gap 0.12) moves 45px left of centre, past the band and short of the 15% line at 57px; Chase for McCaffrey (gap 0.30) and Chase for Rachaad White (gap 0.99) both reach the outer tick at 114px. At 390 (324px SVG, 1.35px per unit) the same three states measure 0, 52, and 130px. The 5% to 15% vocabulary now occupies 19px to 57px of travel at 1440 instead of 5px to 15px, and the words change when the marker crosses a drawn line. On the phone, typing "WR" then "RB" and taking the first option each time lands the rail at 857px in an 844px viewport with focus still in the get combobox, which is the same fold position as this morning, but the sticky strip is now at the top of the screen (358x58 at y 72 under the 73px site header) reading "Balanced offer / limited coverage" with a 77x44 "Evidence" link, so the verdict is visible at the moment it changes. It renders only when both sides hold a player (`trade-result-rail.tsx:83`) and is `display: none` from `lg` up.

4. [P2] The idle rail contradicts the header. Closed. Both source rows print "0/0" over "Waiting for players" before a player is added and "0/1" over the same after one (`trade-result-rail.tsx:248, 266-270`); "Not current" now waits for an evaluated result whose market leg is unusable.

5. [P2] A balanced offer wears the warning tone. Closed. "Balanced offer" measures `rgb(25,24,19)` in light and `rgb(236,234,226)` in dark (`trade-result-rail.tsx:29-35`); leans keep positive and negative, and the warning tone is reserved for the withheld default.

## Priority Issues

1. [P2] The strip covers the rail it links to. Tapping "Evidence" at 390 scrolls the page to 2275 and lands the rail's top at 96px because of `scroll-mt-24` (`trade-result-rail.tsx:203`), while the strip stays pinned at 72 to 130 (`:88`, `sticky top-[4.5rem]`), so the rail's first 34px, the "Evaluation" label and the top half of the coverage chip, sit behind the strip. The strip then stays on screen for the whole time the rail is in view, so the phone shows the verdict title twice, 100px apart, both saying "You are giving more". Why it matters: the jump was added so the phone reader could reach the evidence, and the thing it hides is the coverage chip, which is the one word the note asks them to check. Fix: below `lg` set the rail's scroll margin to the strip's bottom (about 8.5rem) so the anchor lands clear of it, and hide or unpin the strip once the rail's top is above the fold, for instance with an `IntersectionObserver` on the rail or by ending the strip's sticky range at the rail. Suggested pass: layout.

2. [P2] The note is doing three jobs at once. The running-branch note is 115 words against 52 this morning and renders 341px tall at 390 (y 653 to 995, 40% of the viewport), longer than the h1 and its description together, with the two links on its last two lines. It states the model scope, the feed's state and date, the in-season cap, the withholding rule, the first-person promise, and the routing. Why it matters: the honesty framing is protected, but a phone reader has to read fourteen lines before the ledger, and the two sentences that change their behaviour (the cap, and where to go) are the last ones. Fix: keep the dated feed sentence and the cap, move "Once that feed stops updating" into the stale branch where it is true rather than hypothetical, and let the promise sentence go since the cap now demonstrates it. Roughly 70 words carries the same facts. Suggested pass: distill.

3. [P2] The withheld copy still hedges. Unchanged from this morning. "The available inputs are too old, incomplete, or unsupported for a verdict" (`trade-result-rail.tsx:62-66`) still names three causes, and the real cause lives in the warnings behind a closed `details`. The note's stale branch now names the date and the window, so the top of the page will explain a withheld verdict, but the rail itself will not. Still unreachable in production today (every driven state returned a verdict). Fix as before: pick the body from the first warning. Suggested pass: clarify.

4. [P2] Timestamps carry minute precision and wrap in the rail. Unchanged. The Expert consensus `dd` measures 49px over three lines with "AM" alone on the last one against 33px for Draft market, at 1440 in both themes (`formatUpdatedAt`, `fantasyUtils.ts:20-27`). Date-only in the rail. Suggested pass: polish.

## Minor Observations

- The kicker "Preseason redraft · Model v1" still sits 90px above "Week 1 of the 2026 season." Both are true, and the note now explains the pairing, so it reads as scope rather than contradiction; a reader who skips the note still meets the two side by side.
- Chase for McCaffrey (gap 0.30) and Chase for Rachaad White (gap 0.99) draw the same marker at the outer tick. That is the clamp working as designed and the readouts (74.8 against 1.2) carry the difference, but the scale's outer tick has no label saying it means "30% or more", where the inner ticks say 15%.
- The "15%" labels are 8 viewBox units, 9.5px at 1440, at 62% ink, computed near 4.8:1 on the light rail ground. Legible on the screenshots, but they are the smallest type on the page.
- The strip sits at y 72 under a header whose bottom edge is at 73, so its top border rides 1px under the header's hairline. Not visible at 1x.
- After the "Evidence" anchor jump focus lands on `body`; the next Tab does continue from the rail because Chromium moves the sequential focus start point, so Sam is not sent back to the top, but nothing is announced on arrival.
- The freshness row at 390 still wraps to three lines with each "·" at a line end, the same cosmetic note as this morning.
- The 390 tab order runs skip link, site header, breadcrumbs, "View rankings", "weekly board", "waivers page", "Evidence", then the scoring radios, so the strip's link is reachable before the form and is on screen when it takes focus.
- The verdict heading in the rail is still a `<p>` (`trade-result-rail.tsx:219`), so the rail's only heading remains "Evidence mix" in a headings list.
- The centre column at 1440 with one player a side and the four-column fit table for a one-for-one are as they were; neither was in scope for this pass.
- The client test suite now pins the clock at 2026-08-10 and 2026-09-11, and the withheld branch has a test with a Sep 5 stamp, which is the fixture the morning critique asked for in its fourth question.
