---
target: /
total_score: 21
max_score: 28
na_heuristics: 5,7,10
p0_count: 0
p1_count: 0
target_identity: "file:/Users/isaacvazquez/Website/route:"
timestamp: 2026-09-14T18-48-54Z
slug: route
---
Method: re-score after remediation. A: design review subagent (post-fix) · B: computed-value sweep subagent (post-fix), synthesized in the parent. Run 2026-09-14 on branch design/catalog97-loop.

Mode: Persuade (unchanged). Baseline for this run is `2026-09-14T18-34-43Z__route.md`, scored 17/28 before remediation.

## Design health score

| # | Heuristic | Score | Key issue now |
|---|---|---|---|
| 1 | Visibility of system status | 3 | Every readout says when it is from ("Latest quake · 1 d ago", "SPY move on Aug 14 · -0.20%", "Next launch Sep 16 · Falcon 9 Block 5"). The SPY snapshot's last close is still 2026-08-14, so the band shows a month-old number, but it says so. |
| 2 | Match system / real world | 3 | The art-direction caption is gone and the table column reads "Category". "Committed snapshots across three production tools" is still builder vocabulary for a peer. |
| 3 | User control and freedom | 3 | Unchanged. |
| 4 | Consistency and standards | 3 | The count appears twice ("All 33 projects", "All 33") and both are true. Plate "02" still sits beside item ordinals 01 to 03, and the new $4M figure sits inside "Selected work", whose three items are personal tools. |
| 5 | Error prevention | n/a | No inputs. |
| 6 | Recognition rather than recall | 3 | Unchanged. |
| 7 | Flexibility and efficiency | n/a | Persuade surface. |
| 8 | Aesthetic and minimalist design | 3 | The chocolate band dropped one of four unrelated readouts and the table now shows five categories, but a quake, SPY and a launch still share a band titled "Live dashboards". |
| 9 | Error recovery | 3 | A stale snapshot now discloses its date instead of passing as today's. The snapshot itself is still stale. |
| 10 | Help and documentation | n/a | Persuade surface. |
| Total | | 21/28 | Good (75%). n/a: 5, 7, 10 |

## Resolved since the pre-fix snapshot

[P1] The SPY readout was 31 days stale and undated. Resolved as a presentation defect. The sweep read the kicker "SPY move on Aug 14 · -0.20%" at 320, 390 and 1440, and the launch kicker "Next launch Sep 16 · Falcon 9 Block 5 · Space Launch Complex 4E". The last price row in `public/data/investments/SPY/snapshot.json` is still dated 2026-08-14 (read on 2026-09-14), so the data problem carries forward below at P2.

[P1] The hero claimed all 33 tools run on self-refreshing data. Resolved. The hero now reads "I came to product through quality engineering at Civitech, and every tool here is live, with the ones that read outside data refreshing on a schedule." The design review spot-checked News Pulse, which fetches at request time with a 300 second revalidate, and did not check every outside-data tool.

[P1] The page argued from tool count and showed no track record. Resolved. The "33 / Live tools in production" numeral is gone (sweep found no such text at 320, 390 or 1440), the hero names the quality-to-product path at Civitech, and a "$4M / Added revenue from a pricing strategy I led at Civitech" readout sits in Selected work. Isaac confirmed the career figures on 2026-09-14.

[P2] The hero caption was a leaked art-direction note. Resolved. Zero hero figcaptions and no "35mm" text at any measured width, and the `caption` prop is gone from `Catalog97Home.tsx`.

[P2] Recent writing was chosen by date and showed fantasy football and Formula 1. Resolved. The band lists `dashboards-that-read-committed-files`, `building-a-rent-vs-buy-calculator` and `mapping-the-ai-dev-tool-ecosystem`, with meta categories Software Architecture, Fintech Product and AI Tools. The rule is still newest from the product clusters, which is a date rule and not an editorial pick.

Minor items resolved. The launch readout carries its date. The dashboards table shows one tool per category across five categories, under a "Category" column. The source note's "three production tools" matches the three readout columns.

Shared shell items resolved on this route. Every focus ring measured `border-radius: 0` and `box-shadow: none` on all 36 focusables in both themes (they were a 4px radius with a signal-colour halo before). The header held one height through theme toggle mount, 188.4px at 390, 122.8px at 768 and 114px at 1440, with CLS 0 at 390, 768 and 1440 in both themes and at 1024, against 0.0253, 0.0161, 0.0649 and 0.0488 before. Escape, the close button click and the close button by keyboard all return focus to the Search button at 390 and 1440. Under reduced motion `html` computes `scroll-behavior: auto`. "See the work" ends at 822px of an 844px viewport at 320 and at 709px at 390.

## Priority issues

[P2] The SPY snapshot still ends on 2026-08-14 while the hero says outside data refreshes on a schedule.
The readout is honest about its date now, but Aug 14 on Sep 14 is the one place a peer can see a schedule not running. The investments refresh workflow fix merged as PR #434, so the date should advance after the next weekday run. I did not watch a run succeed.
Fix: confirm the next weekday run commits a new snapshot. If it does not, drop the SPY column until the lane is repaired.
Suggested command: /impeccable harden

[P3] The $4M readout reads as a stat about the listed works and has no evidence link.
`Catalog97Home.tsx` places it in the left column of Selected work. At 390 it renders before the three project entries, so "$4M" is the first thing under Selected work, followed by an investment tool, a news dashboard and a fee calculator that did not earn it.
Fix: link the kicker to /resume or /about, or give the readout its own "Track record" label. Copy needs Isaac's approval.
Suggested command: /impeccable clarify

[P3] The readout band still mixes a quake, a market move and a launch under "Live dashboards".
For this audience it proves the pipeline runs more than it proves judgment.
Suggested command: /impeccable distill

[P3] Readout values precede their labels in DOM order.
Unchanged in the diff. No screen reader was run.
Suggested command: /impeccable harden

[P3] Row title links inside headings have hit areas about 20px tall.
The sweep measured the recent writing title links at 20 to 22px tall at 1024 and 1440, and the Selected work and table title links at 23px. The pattern is pre-existing and WCAG 2.5.8 is relevant, though the spacing exception was not evaluated.
Suggested command: /impeccable harden

## New issues

The only new issue is the $4M placement above. The design review found no voice violations in the changed copy.

## Applied after measurement

These landed after the re-score and sweep and were not re-measured beyond a targeted text and style check. `.c97-kicker` now sets `font-weight: 400`, which touches any kicker on a heading site-wide. Nothing else in the post-measurement cleanups changes this route's rendered copy.

## Deliberate decisions (do not re-litigate)

The header wordmark's hit box overlaps the first nav row by 20px at 320 to 768. It stays, because the row gap that cleared it pushed "See the work" below the fold at 320, and `e2e/homepage.spec.ts` guards that button. At 390 the portrait starts below the fold and the nav wraps to two rows, which is the same kept header trade.

## Regression sweep

Across 58 states on the seven Catalog 97 routes (2 at 320 and 14 at each of 390, 768, 1024 and 1440), 0 AA text contrast failures, 0 horizontal overflow, 1 main and 1 h1 everywhere, 0 heading skips, 0 failed requests and 0 console errors.

## Not checked

Screen reader output, whether the SPY lane now succeeds, and every outside-data tool's refresh path beyond News Pulse and SPY.
