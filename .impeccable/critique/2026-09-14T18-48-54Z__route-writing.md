---
target: /writing
total_score: 22
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
target_identity: "file:/Users/isaacvazquez/Website/route:/writing"
timestamp: 2026-09-14T18-48-54Z
slug: route-writing
---
Method: re-score after remediation. A: design review subagent (post-fix) · B: computed-value sweep subagent (post-fix), synthesized in the parent. Run 2026-09-14 on branch design/catalog97-loop.

Mode: Read (unchanged). Baseline for this run is `2026-09-14T18-34-43Z__route-writing.md`, scored 20/40 before remediation.

## Design health score

| # | Heuristic | Score | Key issue now |
|---|---|---|---|
| 1 | Visibility of system status | 3 | The archive header now reconciles in every driven state. Tab counts stay fixed under a search, and there are still 0 live regions. |
| 2 | Match system / real world | 2 | Ten tabs still mix clusters, a length cut and buckets, and row meta still uses `category` ("Software Development", "Formula 1"). |
| 3 | User control and freedom | 2 | No reset and no URL state. |
| 4 | Consistency and standards | 2 | "Browse by topic" still links into Working Instrument topic pages. |
| 5 | Error prevention | 2 | Filter plus search still produces silent zeros. |
| 6 | Recognition rather than recall | 3 | Unchanged. |
| 7 | Flexibility and efficiency | 2 | No deep links. |
| 8 | Aesthetic and minimalist design | 2 | Twelve controls before the first piece, two flat image fields in the featured band, three closing statement bands. |
| 9 | Error recovery | 2 | Scored while the band above a zero-result view still read "Everything that matches is below." That copy has since changed (see Applied after measurement). The empty state still offers no reset. |
| 10 | Help and documentation | 2 | Tab meanings still unexplained. |
| Total | | 22/40 | Acceptable (55%) |

## Resolved since the pre-fix snapshot

[P1] The "hand someone first" pair was the top of whatever list was showing. Resolved. On the default view the pair is `dashboards-that-read-committed-files` and `building-a-rent-vs-buy-calculator`, the two newest posts carrying a product cluster, under "The two above are my newest pieces on product work, and the rest are below, newest at the top." Under a search, under "Shortest first" and under the PM Workflows filter there is no pair and the band reads "Everything that matches is below." The unused `featured` flag on 14 posts was not used, on purpose.

[P1] The default view led with sports under an h1 about product. Partly resolved. The pair is product writing now. The archive directly below still opens with draft-night, Formula 1 and best ball pieces, and its order is kept by decision, so the remainder is P2.

[P3] The archive header read "206 pieces" over 204 rows. Resolved. The header prints the archive list, which excludes the featured pair. The sweep read "204 pieces" over 30 rows plus "Show the rest of the archive 174" by default (204 rows expanded), "79 pieces" over 30 plus 49 under a search, "206 pieces" under "Shortest first" (206 rows expanded), and "18 pieces" over 18 rows under PM Workflows. The "All" tab reads 206, which is the whole index.

[P2] The archive reveal stranded keyboard users. Partly resolved. Pressing Enter on "Show the rest of the archive" moves focus to the link in row 30, with a visible focus ring and the row in view, at 390 and 1440. The empty state reset, live regions and the second cue on the selected tab are still open.

[P3] "Browse by topic" hit boxes overlapped by 15 to 20px. Resolved. The topic grid row gap is `max(var(--c97-sp-3), 34px)`, and the sweep measured 7 links at 50px with 0 overlapping pairs at 390, 768 and 1440.

[P3] The search placeholder measured 3.02:1 in light. Resolved at 5.4:1 light and 7.26:1 dark.

Voice. "Long enough to be useful, and never a listicle." is removed.

Shared shell items resolved on this route. Focus rings measured radius 0 and no shadow on all 71 focusables in both themes. The header held 188.4px at 390, 122.8px at 768 and 114px at 1440 with CLS 0. Search Escape and close return focus to the Search button at 1440. Under reduced motion `html` computes `scroll-behavior: auto`. The pre-fix console error at 768 is gone.

## Priority issues

[P2] The archive under the product pair still opens with sports and fantasy.
Order kept by decision, so the fix, if one is wanted, is a curated shelf above the archive and not a re-sort.
Suggested command: /impeccable layout

[P2] The empty state has no reset, results changes are silent, and the selected tab is marked by ink alone.
0 live regions on the route. No "Clear search and filters" control.
Fix: add the reset button to the empty state, make the archive count a polite live region, and give the pressed tab a second cue.
Suggested command: /impeccable harden

[P2] Ten mixed-taxonomy tabs, a topic grid that leaves the visual world, and row meta from the 37-value `category` field.
Suggested command: /impeccable distill

[P3] Tab counts do not change under a search.

[P3] Archive title links inside h3 headings have small hit areas.
The sweep measured the archive title links at about 23px tall at 1024 and 1440, and its center-point hit test landed on the enclosing h3 for 22 rows at 390, 30 at 768, 9 at 1024 and 7 at 1440. Pre-existing, WCAG 2.5.8 relevant, mechanism not isolated. The count of under-24px targets at 390 went from 7 to 8, which was not traced.
Suggested command: /impeccable harden

## New issues

[P3] A zero-result view put "Everything that matches is below." directly above "0 pieces". Addressed after measurement, below.

## Applied after measurement

When a search or filter empties the archive, the band now reads "Nothing here matches that yet, so try another filter or clear the search." The closing line dropped "not by keyword" and reads "Everything here is tagged by what it is actually about." `.c97-kicker` sets `font-weight: 400`. None of these was re-measured beyond a targeted text and style check.

## Deliberate decisions (do not re-litigate)

The archive order after the featured pair is unchanged. The `featured` flag on 14 posts stays unused, because 13 of those 14 are guides dated 2025-01-24 and picking from it would not be a real choice.

## Regression sweep

Across 58 states on the seven Catalog 97 routes (2 at 320 and 14 at each of 390, 768, 1024 and 1440), 0 AA text contrast failures, 0 horizontal overflow, 1 main and 1 h1 everywhere, 0 heading skips, 0 failed requests and 0 console errors. Default page height is 9,371px at 1440 and 11,852px at 390.

## Not checked

Screen reader output, the empty-state copy in a browser, and the field borders (1.42:1 light and 1.51:1 dark before, not in the diff).
