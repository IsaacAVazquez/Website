---
target: /investments
total_score: 24
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 2
timestamp: 2026-08-06T03-13-04Z
slug: investments
---
Method: dual-agent (A: a476e36d62761795c · B: af11e4ac15f73f98f)

#### Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | Four different as-of values: "Updated 7h ago", "SNAPSHOT · JUL 21", "Curated snapshot · Jul 21", "Snapshot as of Jul 20" in research. |
| 2 | Match System / Real World | 3 | Panel captioned "Your numbers" holds numbers the visitor never entered. |
| 3 | User Control and Freedom | 2 | Header theme toggle is a visible no-op here; per-holding controls hover-gated at opacity 0. |
| 4 | Consistency and Standards | 2 | Two role=tablist groups built to different standards 200px apart; two Refresh; three Add Holding. |
| 5 | Error Prevention | 2 | Untouched Add Position form renders a green --home-positive border on the optional date field. |
| 6 | Recognition Rather Than Recall | 3 | All 8 stat tiles carry real definitions; assumption grid names every input. |
| 7 | Flexibility and Efficiency | 2 | One keyboard shortcut on the surface, and it opens the wrong thing. |
| 8 | Aesthetic and Minimalist Design | 3 | Disciplined terminal identity, undercut by a ~500x370 permanently empty chart panel. |
| 9 | Error Recovery | 2 | Form errors properly wired, but two footer jump links scroll nowhere on an empty portfolio. |
| 10 | Help and Documentation | 3 | Best-served heuristic: assumption disclosure, CMA provenance, per-stat definitions. |
| **Total** | | **24/40** | **Acceptable** |

All ten heuristics applied. Cognitive load: 5 of 8 checks fail (HIGH). Decision points above 4 options: performance timeframe (5), research sections (7), sidebar (7), retirement accordions (5), stat tiles (8), assumptions grid (11). 89 visible interactive elements in the populated state.

#### Design Specificity Verdict

**LLM assessment.** Grounded but uneven. The disclosure layer is unmistakably this product and could not be copied without copying the values behind it. The retirement levers panel is real decision support rather than a chart with a caption. Against that, the hero is stock portfolio-tracker furniture: the 1W/1M/3M/1Y/ALL row is inherited from products with tick history, while this product saves one point per visit with no backfill, so all five resolve to "Not enough saved points in this range yet." The most prominent control contradicts the honesty the rest of the page is built on. Three consecutive bands use an identical shell, so the retirement engine is framed exactly like the empty research placeholder.

**Deterministic scan.** 10 findings, all verified FALSE POSITIVE by an isolated agent, reconciling exactly with the surface brief: 5 display clamps plus the 26px gauge numeral, 3 x border-radius 1px hairline ticks, 1 per-holding colour tick. The detector scores this surface against DESIGN.md's light site-wide ramp, which by design does not govern it. Every real finding in this critique came from review, not from the scanner.

**Visual overlays.** None. The site ships an enforced CSP (script-src 'self' 'unsafe-inline'...) which blocked both the script-src injection and the fetch fallback. The scan API was reached by a different route; the live server was stopped and port 8400 confirmed free. No source file was modified.

#### Overall Impression

The mechanical floor is genuinely strong and the honesty layer is the best thing on the site. What holds the score down is that the surface computes and displays a great deal before the visitor has given it anything, and that the two most prominent moments (the hero chart and the retirement verdict) both speak about data that does not exist yet.

#### What's Working

1. The disclosure layer states limitations the visitor can act on rather than hedges that protect the author.
2. The populated-state transformation grows structure rather than filling holes; the empty state is a first-class state.
3. Verified mechanical floor: 0 contrast failures across 337 nodes (5.77 min inside the terminal), 0 unnamed interactive elements of 84, 0 unnamed sections of 11, one main, one h1, 0 duplicate ids, 0 console errors.

#### Priority Issues

- **[P0] Holdings row actions invisible and unreachable on touch.** `.invest-row-actions` is opacity 0, revealed only by :hover/:focus-within. At 390px the table is 695px inside a 356px scroller, putting the actions cell 339px of nested scroll away at zero opacity with no hover and no Tab. Research, edit and remove are impossible on a phone, and the Research empty state instructs "Click Research on any holding above." Fix: add `.invest-row-actions { opacity: 1 }` to the existing `@media (pointer: coarse)` block and make the column `position: sticky; right: 0`. Suggested command: /impeccable adapt
- **[P1] The Cmd-K badge opens the wrong overlay.** StaticHeader.tsx:61 and InvestmentsDashboard.tsx:97 both bind a window keydown and neither stops propagation, so site-wide search wins and covers the dashboard. The badge advertises the opposite. Suggested command: /impeccable harden
- **[P1] Retirement planner ships a failing verdict labelled "Your numbers."** createDefaultPlan() renders 41/100, NEEDS ATTENTION, -$137.4K, "money runs out age 79" to a visitor who entered nothing, with the reassurance line placed after the verdict. Relabel "Example scenario" until a field is edited. Suggested command: /impeccable clarify
- **[P2] Freshness told four ways; two controls duplicated.** Three as-of statements in the top 130px, a fourth with a different date in research, two Refresh controls 350px apart, three Add Holding entry points. Suggested command: /impeccable distill
- **[P2] Three layout defects at real widths.** Assumptions grid collides text at 1512px (MacBook Pro 14 default), overflowing a cell by 30px inside the compliance-relevant disclosure. Research heading scrolls 13px behind the sticky header (`.invest-research-band` computes scroll-margin-top 0). Stats footer carries a second hardcoded link row with the dead-link defect already fixed in navItems. Suggested command: /impeccable layout

#### Persona Red Flags

**Alex (power user).** The one shortcut he hunts opens site-wide search. Performance tablist has roving tabindex on 0 of 5 tabs while the research tablist below implements it correctly. No bulk select, no multi-remove, no saved views; CSV export is the only bulk affordance and is read-only. The Research jump lands 13px behind the sticky header on every navigation.

**Sam (screen reader / keyboard).** The performance tablist is a fake tablist: 5 elements with role=tab and aria-selected, 0 aria-controls, no role=tabpanel anywhere. The focus glow uses the wrong palette because .terminalScope redeclares --color-primary but never --border-accent, so the ring is #FF6B3B wrapped in a glow built from light-mode #C93F19. `#add-date` computes --home-positive as its border while `#add-cost` with an identical class string computes --home-rule, a colour-only "validated" signal on an untouched optional field. The holdings table is 695px inside a 356px scroller at 390px, the pattern that breaks worst at 200% zoom.

#### Minor Observations

- Nine prose em dashes in shipped UI copy, at RetirementPlanner.tsx:44, RetirementVerdict.tsx:109, RetirementInputs.tsx:244, RetirementAssumptions.tsx:34/35/87, levers.ts:192/208. WRITING_VOICE.md makes this a hard rule and CLAUDE.md makes it binding for UI text.
- Two h2s render larger than the page h1 (34px and 44px vs 24px), both in shared components below the dashboard. Heading levels nest correctly, which is why a level-jump check misses it.
- The header theme toggle is present and focusable on this route, updates its own label, and changes nothing inside the always-dark scope.
- Empty research grid cells render as blank raised rectangles that read as missing data.
- "+107.33% all time" appeared on a position created ten seconds earlier.
- Accent semantic overload: within one viewport #FF6B3B means primary action, held-position status, data-quality caution, allocation magnitude, and system status simultaneously. This is a different claim from the rejected "accent is absent" finding and is worth deciding separately.

**Two corrections to prior work in this session.**
1. The surface brief states the three 1px radii are "documented in both DESIGN.md and the module header." DESIGN.md contains no 1px entry (scale starts at sm 2px) and the module header says "2px corners everywhere," contradicting its own declarations. Brief corrected.
2. The adapt pass fixed the dead #allocation and #holdings-list links in navItems but missed a second hardcoded link row at PortfolioStatsGrid.tsx:236-249. Those two links are still dead on an empty portfolio. The fix was incomplete.

#### Questions to Consider

1. If the data model is one point per visit with no backfill, why does the hero ship a five-range time selector at all? What would a performance panel designed for a visit log look like?
2. The disclosure layer is the most distinctive thing here and renders in 11px mono in the least prominent position. What if provenance became the interface rather than the footnote, so "121 of 151 securities are on older snapshots" were clickable?
3. Should the retirement planner compute anything before the visitor gives it a number?
4. The stats grid answers eight questions with equal weight. Which one actually matters in three seconds?
5. Why does an instrument end in a marketing band rather than a session summary or an export?
